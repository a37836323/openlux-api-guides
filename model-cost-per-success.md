---
layout: default
title: "模型选型为什么不能只看每百万 Token 单价？我更建议算“每个成功任务成本”"
description: "用每个成功任务成本选择大模型"
permalink: /model-cost-per-success/
---

# 模型选型为什么不能只看每百万 Token 单价？我更建议算“每个成功任务成本”

如果团队正在选大模型，最常见的表格通常只有三列：输入单价、输出单价、上下文长度。然后大家自然地选择“看起来最便宜”的模型。

这个方法对简单翻译或格式转换可能够用，但对销售线索研究、合同审阅、代码修改、结构化抽取等真实任务，经常会得出错误结论。

原因很简单：企业真正购买的不是 token，而是**成功完成的任务**。

一个模型单次调用便宜，但需要反复重试、人工返工，或者经常生成无法使用的结果，它的真实成本可能更高。更实用的指标是：

```text
每个成功任务成本
=（API 总支出 + 失败重试浪费 + 人工复核/返工成本）÷ 成功任务数
```

这不是财务上的精确成本模型，但足以帮助团队避免“单价最低就是最省钱”的误区。

## 先把“成功”定义清楚

在计算成本以前，必须先规定什么叫成功。否则不同模型的结果无法比较。

以“从公司官网提取销售线索”为例，成功标准可以是：

- 公司名称、地区、主营产品均有来源支撑；
- 联系方式不能编造；
- 输出符合指定 JSON Schema；
- 关键字段准确率达到预设标准；
- 人工在 2 分钟内可以确认和入库。

如果只判断“接口返回了 200”，几乎所有请求都算成功，但这对业务没有意义。技术成功和业务成功必须分开。

## 一个假设案例：贵一点的模型反而可能更省

下面使用 OpenLux 当前公开页面上的三档基础价格做演算。页面在不同时间、分组或渠道下可能存在倍率差异，正式决策前应重新查询实时价格。

假设每个任务平均使用 10,000 个输入 token 和 2,000 个输出 token，暂不计算缓存折扣。根据当前公开基础价，单次 API 成本如下：

| 示例模型 | 输入价（每百万 token） | 输出价（每百万 token） | 单次 API 成本 |
| --- | ---: | ---: | ---: |
| gpt-5.6-luna | $0.20 | $1.20 | $0.0044 |
| gpt-5.6-terra | $2.00 | $12.00 | $0.0440 |
| gpt-5.6-sol | $5.00 | $30.00 | $0.1100 |

计算方法以 `terra` 为例：

```text
10,000 ÷ 1,000,000 × $2
+ 2,000 ÷ 1,000,000 × $12
= $0.044
```

只看到这里，`luna` 明显最便宜。但现在加入一组**纯假设的任务成功率**，再假设每个失败任务平均需要 $0.20 的人工复核和返工成本：

| 示例模型 | 假设成功率 | API 成本/任务 | 失败返工期望成本/任务 | 估算每个成功任务成本 |
| --- | ---: | ---: | ---: | ---: |
| gpt-5.6-luna | 60% | $0.0044 | $0.0800 | $0.1407 |
| gpt-5.6-terra | 85% | $0.0440 | $0.0300 | $0.0871 |
| gpt-5.6-sol | 96% | $0.1100 | $0.0080 | $0.1229 |

公式是：

```text
（单次 API 成本 + 失败概率 × 单次返工成本）÷ 成功率
```

在这组假设里，中间档反而最省。但这绝不是说 `terra` 在所有任务上都最好。只要成功率、token 用量或人工成本变化，结果就可能改变。

这个例子真正想说明的是：**模型价格是公开参数，任务成功率和返工成本才是你自己的核心数据。** 没有真实评测，不能从价格表直接推导最佳模型。

## 怎样用 20～50 条真实任务做一次可用评测？

不需要先建设复杂评测平台。用一个表格和一批真实样本，就能得到比“凭感觉试几个问题”可靠得多的结果。

### 1. 从真实业务中抽样

选择 20～50 条已经发生过的任务，包含简单、普通和困难样本。不要只挑模型容易回答的案例，也不要用测试人员临时编造的整齐数据。

需要移除个人隐私、商业秘密和不应发送给第三方的内容。

### 2. 固定输入和输出标准

所有候选模型使用相同的系统提示、用户输入、输出格式和工具权限。温度等参数也要一致，否则比较的是两套方案，而不只是两个模型。

### 3. 先盲评质量，再看价格

把模型名称隐藏，让业务人员按照统一标准判断：可直接使用、轻微修改、必须重做、事实错误。先看质量可以减少“贵的应该更好”或“便宜的更划算”带来的主观偏差。

### 4. 记录完整成本

至少记录这些字段：

| 字段 | 为什么要记录 |
| --- | --- |
| 输入/输出 token | 计算直接 API 成本 |
| 缓存命中量 | 长提示词任务可能显著受缓存价格影响 |
| 请求次数与失败次数 | 发现重试造成的隐性浪费 |
| 首次可用率 | 衡量无需返工就能交付的比例 |
| 人工修改分钟数 | 将“质量差一点”换算成真实成本 |
| 延迟 P50/P95 | 避免平均值掩盖长尾等待 |
| 严重错误数 | 幻觉、漏项或违规结果不能被平均分掩盖 |

人工成本不必计算到小数点后很多位。只要所有模型采用同一估算标准，就能支持相对比较。

### 5. 把高风险错误单独处理

医疗、法律、财务、高价值销售决策等场景，不应该只比较平均成功率。一次严重事实错误的损失，可能远高于数万次 token 成本。

可以设置硬门槛：候选模型只要出现特定严重错误，就不能自动交付，必须增加检索、规则校验或人工审核。

## 不必只选一个模型：按任务风险分层更实用

很多团队在“全用便宜模型”和“全用最强模型”之间二选一。更好的方式通常是分层路由：

| 任务层级 | 典型任务 | 建议策略 |
| --- | --- | --- |
| 低风险、高频 | 分类、改格式、简单摘要、关键词抽取 | 先用低成本模型，格式校验失败再升级 |
| 中等复杂度 | 线索研究、内容起草、多条件信息整理 | 使用通过真实评测的主力模型 |
| 高价值、高风险 | 合同结论、重要代码变更、客户最终方案 | 使用高能力模型，并增加来源验证或人工复核 |

OpenLux 的公开页面目前将 `luna`、`terra`、`sol` 展示为不同价格档位，可以作为候选池，但具体能力边界必须用自己的任务验证，不能只根据名称或价格推断。

路由也不要一开始就做得太复杂。最小方案可以是：

1. 先判断任务风险和价值；
2. 选择对应档位的模型；
3. 用 JSON Schema、必填字段和事实来源做程序校验；
4. 校验失败时只升级一次；
5. 高风险结果始终保留人工审核。

这样比“任何失败都反复调用同一模型”更容易控制成本。

## 还有四个经常漏算的成本

### 1. 输出长度

很多模型的输出 token 比输入 token 更贵。提示词如果只写“请详细说明”，模型可能生成大量对业务无用的文字。规定字段、篇幅和停止条件，常常比单纯更换便宜模型更有效。

### 2. 重试和超时

失败请求也可能消耗速率配额和工程时间。临时限流可以有限重试；余额、权限和参数错误不应重试；流式输出已经开始后更不能直接无脑重放。

### 3. 人工等待时间

一个模型虽然准确，但 P95 延迟太高，客服或销售人员每次都要等几十秒，也会形成真实的人力和体验成本。交互场景应同时设置质量与延迟门槛。

### 4. 供应与迁移成本

如果提示词、工具调用和结构化输出高度绑定某个专有行为，未来切换模型的工程成本也要考虑。使用统一接口能减少一部分接入工作，但不能替代回归测试。

## 一个可以直接执行的选型清单

如果下周就要做模型选型，我会按这个顺序：

1. 写出任务的业务成功标准；
2. 抽取 20～50 条脱敏真实样本；
3. 选择低、中、高三个价格档候选模型；
4. 固定提示词、参数和输出格式；
5. 盲评首次可用率与严重错误；
6. 记录 token、重试、延迟和人工修改分钟数；
7. 计算每个成功任务成本；
8. 给低、中、高风险任务分别设默认模型；
9. 小流量上线一周，使用生产数据复核；
10. 价格或模型版本变化后重新评测。

最终选型结果最好不是“某模型永远最好”，而是一条可验证的规则，例如：

> 简单信息抽取默认使用低成本模型；结构校验失败后升级到主力模型；涉及金额、合同义务或对外发送的结果必须人工确认。

这条规则能被工程实现，也能被业务审计。

## 结论

每百万 token 单价只能回答“这次调用理论上多少钱”，不能回答“完成这项工作究竟多少钱”。真正影响决策的是首次可用率、人工返工、重试浪费、延迟和严重错误风险。

先用真实任务测出这几个数字，再计算每个成功任务成本。你会更容易找到适合自己的模型组合，也更容易向团队解释为什么某些任务应该便宜优先，另一些任务值得为质量付费。

如果需要在同一兼容接口下对三档模型做小规模对照测试，可以通过下面的入口注册 OpenLux，并为评测创建独立测试 Key 和预算上限：

https://api.openlux.ai/register?channel=c_lkv0gzwj&utm_source=openlux_api_guides&utm_medium=owned_content&utm_campaign=cost_per_success&utm_content=footer

请以测试当天的公开价格和实际渠道倍率为准。本文表格中的成功率、返工成本和评测结果均为解释计算方法而设置的假设数据，不代表模型能力结论。

## 参考资料

- OpenLux 当前模型、价格与支持接口：https://api.openlux.ai/api/pricing
- OpenAI API 速率限制建议：https://developers.openai.com/api/docs/guides/rate-limits
- OpenAI API 错误说明：https://developers.openai.com/api/docs/guides/error-codes
